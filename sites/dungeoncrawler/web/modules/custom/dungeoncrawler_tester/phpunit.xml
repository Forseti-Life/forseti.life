<?xml version="1.0" encoding="UTF-8"?>
<!--
  PHPUnit configuration for Dungeon Crawler tester module.

  Usage:
    cd sites/dungeoncrawler
    ./vendor/bin/phpunit -c web/modules/custom/dungeoncrawler_tester/phpunit.xml

  Suites:
    - unit: Isolated service/unit coverage
    - functional: Browser tests for controllers/routes/workflows

  Note: failOnPhpunitDeprecation is set to false to allow tests to pass
  despite PHPUnit 11 deprecation warnings. These warnings should be addressed
  in a future update when migrating to newer PHPUnit/Drupal versions.
--> 
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.5/phpunit.xsd"
         bootstrap="../../../core/tests/bootstrap.php"
         colors="true"
         failOnPhpunitDeprecation="false">
  
  <!-- Test Suites -->
  <testsuites>
    <testsuite name="unit">
      <directory>tests/src/Unit</directory>
      <exclude>tests/src/Unit/Traits</exclude>
    </testsuite>

    <testsuite name="functional">
      <directory>tests/src/Functional</directory>
    </testsuite>
  </testsuites>

  <source>
    <include>
      <directory suffix=".php">src</directory>
    </include>
    <exclude>
      <directory>tests</directory>
    </exclude>
  </source>
  
  <!-- Code Coverage -->
  <coverage>
    <report>
      <html outputDirectory="tests/coverage/html"/>
      <text outputFile="tests/coverage/coverage.txt"/>
    </report>
  </coverage>
  
  <!-- PHP Environment -->
  <php>
    <!-- Drupal test database -->
    <env name="SIMPLETEST_DB" value="mysql://drupal_user:drupal_secure_password@127.0.0.1:3306/dungeoncrawler_dev"/>
    
    <!-- Base URL for functional tests -->
    <env name="SIMPLETEST_BASE_URL" value="http://localhost:8080"/>
    
    <!-- Simpletest file storage -->
    <env name="SIMPLETEST_FILE" value="/tmp/dungeoncrawler-simpletest"/>
    <env name="SIMPLETEST_FILES" value="/tmp/dungeoncrawler-simpletest"/>
    <env name="SIMPLETEST_FILES_DIRECTORY" value="/tmp/dungeoncrawler-simpletest"/>
    
    <!-- Browser output directory -->
    <env name="BROWSERTEST_OUTPUT_DIRECTORY" value="/tmp/dungeoncrawler-simpletest/browser_output"/>
    
    <!-- Memory limit -->
    <ini name="memory_limit" value="512M"/>
    
    <!--
      IMPORTANT: Functional tests also require write access to web/sites/simpletest/
      This directory stores temporary test site configurations (settings.php).
      
      For local/CI testing environments, use 777 permissions:
        chmod 777 web/sites/simpletest
      
      For more restricted environments, use 775 and ensure your user is in the web server group:
        sudo usermod -a -G www-data $USER
        chmod 775 web/sites/simpletest
      
      The directory contents are gitignored and should be cleaned after test runs.
      Use the prepare-test-env.sh script to automate this setup.
    -->
  </php>
  
</phpunit>
