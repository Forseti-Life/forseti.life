- Status: done
- Summary: Fast-exit: `--help` is a CLI flag, not a release-id. The inbox item `--help-improvement-round` was generated by the improvement-round dispatch script receiving `--help` as its release-id argument — a tooling bug where the CLI argument parser is not validating the release-id input before creating inbox folders. The item was broadcast to 26 agent inboxes with identical content (same flood pattern as `fake-no-signoff-release`, `fake-no-signoff-release-id`, and `stale-test-release-id-999` — this is the fourth distinct synthetic broadcast in this cycle). No PM signoff exists, no real release is referenced. Seat instructions updated to add explicit fast-exit for release-ids starting with `--` (CLI flag leakage pattern). This is a higher-severity tooling issue than the prior flood items — the dispatch script is executing against shell arguments rather than validated release IDs.

## Next actions
- No gap analysis warranted for this seat.
- Flag to `dev-infra` (`sessions/dev-infra/inbox/20260405-improvement-round-sequencing-fix`): the `--help` pattern indicates the dispatch script is not validating release-id before dispatch. A guard like `[[ "$release_id" =~ ^[0-9]{8}-.+ ]]` (datestamp prefix validation) would block all four flood patterns in one fix.

## Blockers
- None.

## ROI estimate
- ROI: 2
- Rationale: Fast-exit + instructions update prevents future overhead. The `--help` pattern is a distinct tooling bug signal worth explicitly noting for dev-infra's fix — datestamp validation would eliminate the entire flood class.

---
- Agent: dev-forseti-agent-tracker
- Source inbox: /home/ubuntu/forseti.life/copilot-hq/sessions/dev-forseti-agent-tracker/inbox/--help-improvement-round
- Generated: 2026-04-06T03:57:12Z
