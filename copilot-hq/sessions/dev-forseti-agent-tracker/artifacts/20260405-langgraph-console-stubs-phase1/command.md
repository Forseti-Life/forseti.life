- Agent: dev-forseti-agent-tracker
- Status: pending
- command: |
    LangGraph Console Stubs — Phase 1 live implementation (dev-forseti-agent-tracker)

    CONTEXT: The LangGraph dashboard at /admin/reports/copilot-agent-tracker/langgraph has
    several pages now reading real HQ data. The telemetry integration is functional (ticks +
    parity files now use the correct schema). This item covers the next layer: making the
    remaining pages richer by reading more HQ data.

    CURRENT STATE (as of 2026-04-05 telemetry fix):
    - langGraphSessionMonitoring() — LIVE: reads langgraph-ticks.jsonl ✓
    - langGraphParityHealth() — LIVE: reads langgraph-parity-latest.json ✓
    - langGraphReleaseStatus() — LIVE: reads langgraph-ticks.jsonl for publish controls ✓
    - langGraphReleaseTroubleshooting() — LIVE: reads copilot_agent_tracker_agents DB table ✓
    - langGraphReleaseNotes() — LIVE: calls $this->releaseNotes() (DB) ✓
    - langGraphFeatureProgress() — LIVE: reads dashboards/FEATURE_PROGRESS.md ✓
    - buildLanggraphTroubleshootingPanels() — LIVE: reads ticks + parity + orchestrator log ✓

    PHASE 1 PRIORITIES (highest ROI first):

    1. **Auto-refresh FEATURE_PROGRESS.md from feature files** (ROI: 7)
       - File: copilot-hq/dashboards/FEATURE_PROGRESS.md
       - Currently: static file last updated 2026-02-20
       - Needed: a script (scripts/generate-feature-progress.sh or Python) that reads
         features/*/feature.md and updates FEATURE_PROGRESS.md with current status
       - Acceptance criteria: FEATURE_PROGRESS.md is auto-generated by orchestrator tick
         (add a step or cron call); the dashboard feature table shows current data
       - Verification: check that feature rows in /admin/reports/copilot-agent-tracker/langgraph/feature-progress
         match the current state of features/ directory

    2. **Provider field in tick record** (ROI: 5)
       - File: copilot-hq/orchestrator/runtime_graph/engine.py _write_tick_telemetry()
       - Currently: tick["provider"] = str(state.get("provider", "")) returns ""
         because `provider` is not written to state (it's a parameter to run_tick, not in state dict)
       - Needed: pass `provider` identity (e.g. provider class name) into state before
         _write_tick_telemetry is called, so the Session Health table shows the provider column
       - Acceptance criteria: tick["provider"] shows a non-empty value in the dashboard table
       - Verification: check tail of langgraph-ticks.jsonl after one orchestrator run;
         confirm "provider" field is non-empty

    3. **Orchestrator log mode detection improvement** (ROI: 4)
       - File: DashboardController.php buildLanggraphTroubleshootingPanels()
       - Currently: detects "langgraph" mode by checking if log contains '"steps"' substring
         (line ~509). Now that log format changed to use step_results, this check may
         always return "unknown" for the engine_mode field.
       - Needed: update detection logic to check for '"step_results"' or '"dry_run"' in the
         orchestrator log, OR read mode from langgraph-ticks.jsonl directly (preferred)
       - Acceptance criteria: engine_mode in troubleshooting panel shows "langgraph" not "unknown"
       - Verification: load /admin/reports/copilot-agent-tracker/langgraph (main dashboard)
         and check "Engine mode" row in the LangGraph Runtime Status table

    ACCEPTANCE CRITERIA (all 3 items):
    - FEATURE_PROGRESS.md is refreshed automatically and dashboard shows current features
    - provider field shows non-empty string in session health tick table
    - engine_mode shows "langgraph" in troubleshooting panel

    FILES IN SCOPE:
    - copilot-hq/orchestrator/runtime_graph/engine.py (provider + optionally feature gen)
    - copilot-hq/scripts/ (new script: generate-feature-progress.sh or .py)
    - web/modules/custom/copilot_agent_tracker/src/Controller/DashboardController.php (engine_mode detection)

    KB REFERENCE: none found — this is a new feature area.

    ROI: 6 — CEO visibility interface; feature progress and provider clarity are the top
    remaining gaps after telemetry schema fix.
