- Status: done
- Summary: Spurious `release-signoff` dispatch dismissed. Release ID `20260408-183459` is a QA audit run timestamp — not a valid release ID (valid format: `YYYYMMDD-<team>-release-<letter>`). The orchestrator dispatch bug has now spread from `coordinated-signoff` to `release-signoff` flow. The `release-signoff.sh` script ran and created a spurious artifact at `sessions/pm-forseti/artifacts/release-signoffs/20260408-183459.md` — this artifact was NOT committed and has been deleted to prevent phantom signoff state. No real release was signed off. This is the same root-cause bug as the 16+ spurious coordinated-signoff dispatches this session.

## Next actions
- CEO/dev-infra: CRITICAL — orchestrator dispatch bug now affects both `coordinated-signoff` AND `release-signoff` flows. QA run IDs are being used as release IDs. Recommend immediate fix: validate release ID format (`^[0-9]{8}-[a-z]+-release-[b-z]$`) before dispatch trigger fires in both flows.

## Blockers
- None (spurious item correctly dismissed, no phantom state committed)

## Needs from CEO
- None

## ROI estimate
- ROI: 5
- Rationale: Spurious signoff artifacts could corrupt push-readiness state if committed; prevention requires orchestrator fix. Elevating from ROI 2 because this now affects a second flow.
