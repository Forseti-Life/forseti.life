- Status: blocked
- Summary: QA run 20260422-201501 shows the same 3 failures (404 on `/index.php/roadmap/PROJ-002`, `/PROJ-008`, `/PROJ-011`). This is cycle 8 of the same deploy blocker. Confirmed current production state: `mod_rewrite` is active and `/roadmap/PROJ-002` returns 200; the `hook_url_outbound_alter` code from commit `7b31fb415` is present in `modules/custom` via symlink; but Drupal's hook cache has not been rebuilt so it still generates `href="/index.php/roadmap"` on every page. Additionally, the `.htaccess` R=301 redirect from commit `6f82d6e92` is not live because `.htaccess` is not symlinked and `git pull` has not been run on production. Both issues are resolved by two commands: `git pull --rebase origin main && vendor/bin/drush cr`. SSH access from this seat is denied. No automated path is available.

## Next actions
- **Human (2 commands, ~30 seconds)**: `ssh ubuntu@forseti.life` then: `cd /home/ubuntu/forseti.life && git pull --rebase origin main && cd sites/forseti && vendor/bin/drush cr`
- Verify: `curl -s https://forseti.life/ | grep roadmap` → must show `/roadmap` not `/index.php/roadmap`
- QA re-run will then show 0 failures and release-m can close.

## Blockers
- SSH `Permission denied (publickey)` from this seat to `ubuntu@forseti.life` (verified last cycle).
- GitHub Actions secrets `PRIVATE_KEY`, `HOST`, `USERNAME` all unset — deploy.yml cannot run.
- This is cycle 8. No technical work remains. Production has not pulled or cache-cleared.

## Decision needed
- Human owner must run the two commands above, or delegate SSH access to an automated agent.

## Recommendation
- Run manually now. Also restore GitHub Actions SSH secrets so `drush cr` runs automatically on every push going forward, preventing this class of blocker permanently.

## Needs from CEO
- Escalation to Board is overdue (cycle 8). Confirm Board has received and acknowledged the deploy blocker and has committed to acting on it this session.

## ROI estimate
- ROI: 500
- Rationale: Two commands close 8 blocked QA cycles and release release-m. Every additional cycle consumes agent slots with zero new value.
