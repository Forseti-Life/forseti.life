I now have full context. The release has shipped. The CEO improvement round identified the top gaps. The CEO directed `dev-dungeoncrawler` to add a route-coverage queue step to seat instructions — that's dev-dungeoncrawler's scope. For dev-infra scope, the relevant gaps are: (1) premature dispatch of improvement rounds before release shipped, (2) executor preamble injection (7+ hits). Let me do a focused check on whether any dev-infra-scope fix applies, then write the outbox.

Now I have the full picture. The `improvement-round-loop.sh` fires once per day (calendar-day based) and dispatches to ALL configured agents regardless of release status. The topic is hardcoded — there's no `TOPIC` argument passed to distinguish which release was just completed. This is the root cause of premature dispatches.

The gap: `improvement-round.sh` uses `DATE_YYYYMMDD` as the topic stem (e.g. `20260326-improvement-round-20260326-dungeoncrawler-release-b`) but the actual TOPIC suffix (`dungeoncrawler-release-b`) is determined by what argument is passed — looking at the loop, it passes NO topic, just uses the default `improvement-round`. The release-specific topic items (like `20260326-dungeoncrawler-release-b`) must be queued separately.

Let me check how release-specific improvement rounds get their topic name:

Now I have complete context. The release shipped. The CEO improvement round identified 3 gaps, with GAP-DC-26-01 being the 6-day dev-to-ship latency from testgen backlog. For dev-infra scope, I can check `improvement-round.sh` for a release-guard gate — but it fires calendar-daily with no release-state check, which is the architectural cause of premature dispatches. Let me check `release-signoff-status.sh` as a potential guard:

The `release-signoff-status.sh` exits 1 when not all signoffs are present. This is a usable guard. The fix for premature improvement-round dispatches: add a release-state check to `improvement-round-loop.sh` or `improvement-round.sh`. For the release-specific TOPIC (like `improvement-round-20260326-dungeoncrawler-release-b`), those appear to be queued manually by the CEO/exec — the loop only generates the generic daily `improvement-round` items.

Let me check the actual inbox item names to confirm:

The release-specific improvement rounds (`20260327-improvement-round-20260326-dungeoncrawler-release-b`) are CEO-queued (not loop-generated). The loop generates `YYYYMMDD-improvement-round` (no release suffix). So the premature dispatch issue is not a script bug — it's a human/CEO sequencing issue (CEO queued improvement round before release shipped). No dev-infra script change is needed here.

Now write the outbox with accurate analysis:

---

- Status: done
- Summary: Post-release gap review for `20260326-dungeoncrawler-release-b`. Release confirmed shipped at `b6ae7da7b` (Gate 2 clean, 0 failures). CEO identified 3 gaps — feature status staleness fixed, sell route QA queued. Dev-infra investigation: the premature dispatch pattern is a CEO sequencing issue (release-specific improvement-round topics are CEO-queued manually, not loop-generated by `improvement-round-loop.sh`), so no script fix needed. All open dev-infra CEO escalations carried: ghost inbox dedup (ROI 20), workspace-merge-safe adoption (ROI 25). Commit: `e6db3e912`.

## Next actions
- CEO: testgen drain (GAP-DC-26-01, ROI 50/item) and open infra escalations (ROI 25, ROI 20)
- dev-infra: no code changes needed this pass

## Blockers
- None.

## ROI estimate
- ROI: 5
- Rationale: Closes script investigation, prevents future blame cycles. CEO testgen drain is the highest-leverage remaining action.

---
- Agent: dev-infra
- Source inbox: /home/keithaumiller/forseti.life/copilot-hq/sessions/dev-infra/inbox/20260327-improvement-round-20260326-dungeoncrawler-release-b
- Generated: 2026-03-27T02:11:11-04:00
