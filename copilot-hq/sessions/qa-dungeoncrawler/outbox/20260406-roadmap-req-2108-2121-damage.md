Good. Now let me check if `damage_str_mode` is actually wired into the damage calculation in CombatEngine, and run the live tests for the pending items.

The command is likely hanging on the service call. Let me split it into smaller probes without `\Drupal::service`.

All facts are in. Now I have a clear picture of all 14 reqs. Let me write the verification report.

Commit `e1acc55b8`. Outbox update:

---

- Status: done
- Summary: Verified reqs 2108–2121 (Damage Rules) against production. 8 of 14 pass cleanly; 1 logic bug identified (DEF-2114); 5 wiring gaps confirmed. PASS: REQ-2108 (open-ended damage type system, case-insensitive), 2109 (melee `rollDamage` with Str modifier), 2110 (ranged default 0 modifier), 2113 (spell path uses `rollExpression`), 2115 (`applyCriticalDamage` correctly returns dice×2+static), 2117 (resistance/weakness formulas, immunity check), 2120 (nonlethal −2 attack penalty in ActionProcessor), 2121 (nonlethal 0 HP → unconscious in HPManager). FAIL — DEF-2114: `HPManager::applyDamage()` captures `$original_damage` *after* the resistance loop; when resistance fully absorbs incoming damage (e.g. resist 5 vs damage 4), `$original_damage = 0` so the min-1 guard `if ($original_damage > 0 && $damage < 1)` never fires — result is 0 instead of 1. Fix: capture incoming damage before the resistance loop. Wiring gaps (service logic exists but never called by `CombatEngine::resolveAttack`): GAP-2111/2112 — `damage_str_mode` set by `ItemCombatDataService` for thrown/propulsive but CE reads `$weapon['ability_modifier']` directly; GAP-2116 — `CritSpecializationService` implemented but not injected/called on crits; GAP-2118/2119 — `RulesEngine::checkImmunities` handles `critical_hits` and `precision` but CE never calls it during damage resolution. Verification report and regression checklist committed `e1acc55b8`.

## Next actions
- Surface DEF-2114 and wiring gaps (2111/2112/2116/2118/2119) to dev-dungeoncrawler for fixes — these are all in `CombatEngine::resolveAttack()` and `HPManager::applyDamage()`
- Process remaining inbox items: `20260406-impl-flat-check-system`, `20260406-impl-afflictions`, `20260406-impl-areas-of-effect`, `20260406-impl-counteract-rules`

## Blockers
- None (BLOCK verdict is on the release gate, not on my process)

## Needs from CEO
- None

## ROI estimate
- ROI: 80
- Rationale: DEF-2114 causes resist-absorb scenarios to silently deal 0 damage (wrong floor); the 4 CE wiring gaps (thrown/propulsive Str, crit spec, crit immunity, precision immunity) mean every combat fight applies incorrect mechanics — fixing before ship prevents visible gameplay regressions.

---
- Agent: qa-dungeoncrawler
- Source inbox: /home/ubuntu/forseti.life/copilot-hq/sessions/qa-dungeoncrawler/inbox/20260406-roadmap-req-2108-2121-damage
- Generated: 2026-04-06T22:17:40+00:00
