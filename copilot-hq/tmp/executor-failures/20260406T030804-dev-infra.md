# Executor failure: dev-infra

- Agent: dev-infra
- Inbox item: 20260405-scope-filter-improvement-round-dispatch
- Failed at: 2026-04-06T03:08:04+00:00
- Retries attempted: 2
- Failure reason: agent response missing required status header after 2 retries
- Action: no stub outbox written; stagnation detector should query tmp/executor-failures/ for systemic signal

## Raw response (first 500 chars)
The `--dry-run` check is after the `$1` validation. I need to fix the ordering — handle `--dry-run` before the YYYYMMDD validation:

The work is already complete and verified. The outbox already exists with status `done`. All 3 acceptance-criteria checks still pass live:

- `--help` → exit 1 "first argument must be a date in YYYYMMDD format" ✓
- `improvement-round-fake-test` → exit 1 "release-id suffix must start with YYYYMMDD" ✓  
- bare `improvement-round` → exit 1 "bare improvemen
