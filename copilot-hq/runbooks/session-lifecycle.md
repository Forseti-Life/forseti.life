# Session Lifecycle Runbook

1. Create session folder under `sessions/<session-id>/`.
2. Add `summary.md` using `templates/session-summary.md`.
3. Add exports/artifacts only after sanitizing.
4. Mark status in your master tracker (SQLite or issues).
